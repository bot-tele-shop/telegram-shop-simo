services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: velmora_dev
      POSTGRES_USER: velmora
      POSTGRES_PASSWORD: change-me-local-only
    ports:
      - "127.0.0.1:54329:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
